export interface IconProps {
    //State
    fill?: boolean | string,

    //Size
    size?: number,
    strokeWidth?: number,

    //Color
    color?: string,
    backgroundColor?: string,
}

export const defaultSize = 35;
export const defaultColor = 'black';
export const defaultFill = false;
export const defaultBackgroundColor = 'white';
export const defaultStrokeWidth = 1;
